{
  "cli": "nx",
  "id": "test",
  "type": "object",
  "properties": {
    "graphPath": {
      "type": "string",
      "description": "Path to graph",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    }
  },
  "required": [
    "graphPath"
  ]
}
